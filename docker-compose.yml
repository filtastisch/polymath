services:
  polymath:
    container_name: polymath
    build: .
    command: python -u run
    restart: always
    ports:
      - ":8080"
    volumes:
      - polymath-data:/polymath/polymath/storage
    networks:
      - frontend

networks:
  frontend:
    external: true